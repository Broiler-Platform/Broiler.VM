applied W17 to src/Broiler.VM.Runtime/VmRuntime.cs
diff --git a/src/Broiler.VM.Runtime/VmRuntime.cs b/src/Broiler.VM.Runtime/VmRuntime.cs
index 79f7485..0d1a047 100644
--- a/src/Broiler.VM.Runtime/VmRuntime.cs
+++ b/src/Broiler.VM.Runtime/VmRuntime.cs
@@ -726,11 +726,6 @@ public sealed partial class VmRuntime : System.IDisposable
                 return;
             }
 
-            var depth = CapabilityDepth - 1;
-
-            // Null, not zero. Storing zero would leave the entry on this thread forever; storing
-            // nothing is what actually releases it, and releasing it is the whole point.
-            inCapabilityDepth.Value = depth > 0 ? depth : null;
         }
     }
 
