applied W1 to src/Broiler.VM.Runtime/VmRuntime.cs
diff --git a/src/Broiler.VM.Runtime/VmRuntime.cs b/src/Broiler.VM.Runtime/VmRuntime.cs
index cb2b50b..90c2cde 100644
--- a/src/Broiler.VM.Runtime/VmRuntime.cs
+++ b/src/Broiler.VM.Runtime/VmRuntime.cs
@@ -504,8 +504,6 @@ public sealed partial class VmRuntime : System.IDisposable
             // it has already been admitted and not yet committed. A host reading a running runtime
             // is asking what has been spent, and a figure that stopped at the last poll of each
             // operation would answer a different question.
-            runtimeLevel.FuelPreAdmissions!.SettleAll();
-
             return runtimeLevel.Snapshot();
         }
     }
