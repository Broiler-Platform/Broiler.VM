# slice-compiler --checks with only the parser change reverted (the check added, JsParser line 391 back to awaitIsOperator = true), 2026-09-22.
FAIL a module compiled without top-level await refuses it by name, and one compiled with it does not: without, `await null; export const x = 1;` answered bytes where the same construct in a function answers ReservedWordAsBinding:`await` is a reserved word in a module; without, `for await (const v of []) {} export const x = 1;` answered bytes where the same construct in a function answers UnexpectedToken:`for await` is only admitted inside an async function or an async generator; without, `await using r = null; export const x = 1;` answered bytes where the same construct in a function answers ReservedWordAsBinding:`await` is a reserved word in a module
broiler-js-slice-compiler: 1 of 372 checks FAILED, 2 not run on this machine and claimed by nothing
