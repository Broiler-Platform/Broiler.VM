ok   a host method answers an object, and the guest reads a string off it
ok   wrapper identity survives the crossing in both directions
ok   an accessor the host installed runs host code on every read
ok   a host method calls back into the guest, synchronously, before it returns
ok   a listener's throw reaches the host and is re-raised into the guest
ok   an error the host raises is a real guest error the guest can catch
ok   an exotic object answers a name it was never given, and enumerates it
ok   an exotic object answers an index, and enumerates its elements before its names
ok   an exotic object takes the name it claims and lets every other assignment through
ok   the host links a wrapper to a prototype the guest can see
ok   a buffer the host filled reads as half precision in the guest, and back as bytes
ok   a realm in a composition that registered no permission has no host object
ok   the host queues a job beside the guest's own, and a drain runs both in order
ok   the host detaches a buffer the guest handed it, and only an ArrayBuffer
ok   a buffer from another realm is refused rather than detached
ok   an embedder reaches its realm between invocations, and only by asking
ok   the host reads an ArrayBuffer's exact bytes: empty, short and multi-chunk
ok   the host reads into a span it owns, and a short span is written nothing
ok   only the realm's own ArrayBuffer brand is read; a subclass instance is one
ok   a detached buffer answers Detached, and the buffer it moved to answers its bytes
ok   a resizable buffer is read at its current length after it grows, shrinks and moves
ok   replaced globals, prototypes, species and join change nothing either member does
ok   the host makes an ArrayBuffer from its bytes: empty, short and multi-chunk, and a copy
ok   a resize is charged past its high-water mark, and one the live-bytes ceiling refuses ends the run
ok   a read the fuel allowance refuses ends the operation and copies nothing (ResourceExhaustion/AllowanceExhausted)
ok   a cancelled read ends the operation and copies nothing (Cancellation/Cancelled)
ok   a buffer the live-bytes ceiling refuses is never made, after one it admits (ResourceExhaustion/CeilingReached)
ok   a buffer the fuel allowance refuses is never made (ResourceExhaustion/AllowanceExhausted)
ok   a cancelled construction is never made (Cancellation/Cancelled)
ok   a deleting exotic takes a claimed name away, and the ordinary deletion runs after it
ok   an index key and a symbol are never offered to the deletion hook, and 007 is a name
ok   Reflect, a trapless Proxy and the host's own delete each offer a name exactly once
ok   a declined name keeps answering, and a non-configurable ordinary property stays
ok   a deletion hook that throws ends the deletion, and the property is left in place
ok   a deletion hook refused at the seam gives the guest a TypeError and keeps the property
ok   a deletion hook whose guest callback exhausts the allowance ends the run, uncatchably
ok   a host promise settles through the job queue and never synchronously
ok   the first settlement of a host promise wins, and a late one is reported as such
ok   a thenable is adopted the way the engine's own resolve procedure adopts one
ok   a guest replacing Promise changes nothing about how the host builds one
ok   a host promise settled from another CLR thread is refused, and the promise stays pending
ok   Missing is refused as a settlement value, and the promise stays open for a real one
ok   a host promise settles only in a turn of its own realm, late is reported, disposed is refused
ok   a host links a two-module graph, evaluates it once, and reads live bindings
ok   a host-linked cycle meets the cross-module dead zone and then the live binding
ok   a load that fails is a guest error of the right kind, is not cached, and links nothing
ok   top-level await settles a host evaluation only through the queue, and only when finished
ok   an errored module stays errored, and a cycle member waits for its awaiting root, whoever started them
ok   a deferred import stays pending until a turn completes it, once, through the queue
ok   a loader may not complete the request it is offered, and a Now answer loads in the step
ok   a deferred request is refused in another realm and after its realm is released
host-surface: every check passed
