// Witness for rule J1: the block drifted below the declaration it describes.
//
// The two lines sit after the last member, so they are the leading trivia of the
// closing brace and of no declaration at all. Fold is therefore uncovered, which is
// the point: an annotation in the file is not an annotation on the unit.

namespace Probe;

// Broiler-AI:    Origin=AI; IP=Low; Security=Low; Resources=0; Fingerprint=178FC2
// Broiler-Human: PENDING
public static class Drifted
{
    public static int Fold(int[] values)
    {
        var total = 0;

        foreach (var value in values)
        {
            if (value > 0)
            {
                total += value;
            }
        }

        return total;
    }

    // Broiler-AI:    Origin=AI; IP=Low; Security=Low; Resources=3; Fingerprint=TBF
    // Broiler-Human: PENDING
}
