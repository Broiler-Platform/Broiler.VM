A witness for rule J12's fifth clause: a figure bound to a COUNTABLE SUBJECT rather than to
the word "exist", which the fourth clause cannot see.

Every sentence below was in the register. The tree grew when the JavaScript profile came
under assurance coverage and the rows did not: the covered-file count is 61, the artefact
count 64, and 905 of the 1987 code units are annotated.

This paragraph is written so that it does not itself trip the clause - the figures stand
apart from the nouns they count. A witness whose narration fired would report itself, which
is how the first draft of this file failed.

    The 45 covered product source files are compared whole.

    Forty-eight artefacts are in the plan, one per covered product source file.

    689 annotations are parsed and held to their vocabularies.

The sentences below must NOT be reported. The first two count a chosen witness set and the
clauses of a rule rather than anything in the tree, and the third names a record rather than
a quantity - a rule that read them would be asking the register to stop explaining itself.

    A three-annotation unit set is chosen so that a mistake changes the answer.

    Four clauses have witnesses of their own, and two rounds defeated the first shape.

    ADR 0001 revision 5 owns the exemption.
