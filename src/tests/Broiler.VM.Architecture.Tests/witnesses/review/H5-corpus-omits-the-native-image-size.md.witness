# Witness: a corpus that has stopped quoting the Native AOT image size

The suite reported 293 tests passed, of which 99 architecture tests and 194 behavioural tests,
and the fixtures host also runs as a 78,256-byte trimmed binary. No Native AOT size is quoted
anywhere here.
