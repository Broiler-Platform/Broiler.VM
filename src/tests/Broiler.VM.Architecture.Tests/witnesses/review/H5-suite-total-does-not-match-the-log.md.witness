# Witness: a suite total the retained log does not record

The suite reported 222 tests passed, and the split below is the one the log does record, so the
total is the only figure at odds with `docs/evidence/vm-6/test.log`.

The run was 121 architecture tests and 197 behavioural tests.
