# Witness: a suite total the retained log does not record

The suite reported 176 tests passed, and the split below is the one the log does record, so the
total is the only figure at odds with `docs/evidence/vm-1/test.log`.

The run was 44 architecture tests and 131 behavioural tests.
