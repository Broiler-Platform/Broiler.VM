/*---
esid: sec-additive-operators
description: >
  A positive test with no raw flag. The suite prepends its assertion library to every such file,
  whatever the includes line says, so it needs a call before its first statement runs.
---*/
1 + 2;
