# The CLI acceptance suite: one row per command line, judged on the real binary.
#
# args|exit|substring
#
# NOTHING HERE INJECTS ANYTHING. Every row is a file this host is pointed at and an exit code it
# must answer with; no source of this component is patched, no internal is reached for, and the
# thing under test is the published binary rather than a type inside it. A control is an INPUT.
#
# The substring is matched against standard output and standard error together, because which of
# the two carries a message is part of what this host promises and is asserted separately by the
# rows that expect a value on stdout with nothing on stderr.

# It runs what the manifest admits, and prints the completion value and nothing else.
runs/a-product.js|0|42
runs/a-boolean.js|0|true
runs/a-loop-that-ends.js|0|45
--module runs/a-module-goal-program.js|0|3

# --check compiles and verifies without running, so it prints no value.
--check runs/a-product.js|0|

# It refuses source, by name, with the position.
refused/a-const-with-no-initialiser.js|3|2203:ConstWithoutInitialiser
refused/a-string-value.js|3|2104:ConstructOutsideManifest
refused/a-break-outside-a-loop.js|3|2207:IllegalBreak
refused/an-unterminated-string.js|3|2004:UnterminatedStringLiteral
refused/a-free-name.js|3|2206:UnresolvableIdentifier

# THE NESTING BOUND IS A CEILING THIS BUILD DECLARES, not a statement about the language, and the
# same file answers differently either side of it. Two files of the Octane benchmark are refused at
# the default for exactly this reason.
limits/nesting-past-the-default.js|3|2103:NestingTooDeep
--max-depth 512 limits/nesting-past-the-default.js|0|1

# A program with no exit ends by spending its allowance, and says so rather than being killed.
--fuel 200000 limits/a-loop-that-does-not-end.js|5|did not settle within its allowance

# A byte-order mark is format-control whitespace a source text may open with, so an ordinary file
# saved by an ordinary editor runs.
encoding/with-a-byte-order-mark.js|0|7

# BYTES THAT ARE NOT UTF-8 ARE REFUSED RATHER THAN DECODED WITH REPLACEMENT CHARACTERS. A
# replacement character changes the program, and a host that ran a different program than the file
# contains is worse than one that declines.
encoding/not-utf8.js|6|not valid UTF-8

# The command line itself is checked: a path that is not there, and an option this host has not.
missing/nothing-here.js|6|no file or directory at
--nonsense runs/a-product.js|2|is not an option this host has

# Several files at once report the WORST answer, and a legitimate refusal does not outrank a
# clean run into failure - it simply dominates it, because the caller has to know something
# was refused.
runs/a-product.js refused/a-string-value.js|3|RefusedSource: 1

# TWO OF THESE THREE WERE PINNED AS DEFECTS AND ARE NOW REPAIRED, WHICH IS WHAT PINNING THEM WAS
# FOR. They were declared with exit 4 - the code that accuses this component - because the lowering
# emitted a loop's update and back-edge even when a body that always breaks meant nothing reached
# them, and the verifier refused the artifact as 1411:UnreachableCode. Thirteen files of test262
# were exactly this. The rows moved from 4 to 0 with the repair; the suite went red in between.
known-defects/a-loop-whose-body-always-breaks.js|0|
known-defects/a-while-true-whose-body-always-breaks.js|0|

# THE THIRD IS STILL REFUSED AND IS A DIFFERENT SHAPE. This loop has no exit at all, so everything
# after it is unreachable INCLUDING THE PROGRAM'S TAIL - and suppressing a tail leaves a function
# with no terminator, which is a different invalid artifact rather than a valid one. It is the
# shape the compiler's own remark named all along, it remains a documented exclusion, and it is
# pinned separately so that one change cannot quietly claim both.
known-defects/a-loop-with-no-exit-at-all.js|4|1411:UnreachableCode

# THE TEMPORAL DEAD ZONE, both halves and both directions. Exit 1 is a program that threw: these
# are RUNTIME ReferenceErrors, which is what the language says and what this profile could not
# express at all until the format grew an instruction that can fail.
dead-zone/a-read-before-the-initialiser.js|1|ReferenceError
dead-zone/a-write-before-the-initialiser.js|1|ReferenceError
dead-zone/a-read-inside-its-own-initialiser.js|1|ReferenceError

# And the two that must NOT throw. A `var` is initialised when its scope is entered, so reading one
# early is `undefined`; and a lexical binding after its initialiser is unremarkable. A dead-zone
# check that fired too eagerly would break hoisting, and these are what would catch it.
dead-zone/a-var-is-not-in-the-dead-zone.js|0|undefined
dead-zone/a-lexical-binding-after-its-initialiser.js|0|5

# A HASHBANG IS A COMMENT, and only at offset zero. The second row is the one that matters: a `#!`
# anywhere else is still a character that begins no token, so a fix that skipped them everywhere
# would pass the first row and fail this one.
hashbang/at-the-very-start.js|0|3
hashbang/on-the-second-line.js|3|2102:ExpectedToken
