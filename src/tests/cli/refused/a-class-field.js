// A class BODY is admitted and a class FIELD is not, so the refusal has to name the field
// rather than come back as a missing parameter list. Admitting the family around it is
// exactly when that is easiest to lose.
class Counted {
  total = 0;
}
