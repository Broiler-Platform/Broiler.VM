// A free name. In the language this is a run-time ReferenceError; this manifest declares
// no global object, so the answer can never change and is given at compile time. The
// divergence is recorded against diagnostic 2206.
undeclared + 1;
