// THE THIRD NEW POSITION, and it is the one where two families meet. A private name is admitted
// and an async generator is not, so `async *#m` is a member whose KEY this manifest has and whose
// KIND it does not - and the modifier has to be settled before the key is read for the refusal to
// name the async generator rather than the `#`.
class Held {
  async *#pump() {
    yield 1;
  }
}
