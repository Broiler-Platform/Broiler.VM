// A SHORTHAND'S KEY IS ALSO ITS BINDING NAME, so a word strict code reserves is answered for
// as a reserved word - 2209 - and not as a missing colon. The manifest is not what forbids this;
// the language is, and a test asserting the reservation is one this profile can pass.
"use strict";
var { yield } = {};
