// A specifier this host does not resolve: a bare name needs a package layout, a search path
// and a manifest format, and this composition has none of the three.
import { anything } from "somewhere";
anything;
