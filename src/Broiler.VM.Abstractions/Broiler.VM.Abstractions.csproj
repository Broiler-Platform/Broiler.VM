<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>Broiler.VM.Abstractions</RootNamespace>
    <AssemblyName>Broiler.VM.Abstractions</AssemblyName>
    <PackageId>Broiler.VM.Abstractions</PackageId>
    <Description>Profile-neutral Broiler.VM contracts and the core contract version: descriptors, identities, limit vectors, budget vocabulary, transfer types, lifecycle objects and the operation-result envelopes. No runtime, no format, no semantics.</Description>
  </PropertyGroup>

  <!-- ADR 0001: this project has no ProjectReference. It is a sink of the
       dependency graph, which is what makes the graph acyclic by construction. -->

</Project>
