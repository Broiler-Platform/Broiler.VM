# Broiler.VM.Profile.JavaScript decision records

The JavaScript profile's own dated decisions, numbered `JSD-nnnn`. They are a **separate series
from the core's ADRs** and neither series numbers into the other: a `JSD` record decides
something about this profile, an `ADR` decides something about the Broiler.VM core, and where a
decision needs both halves each record carries its own and names the other.

The [roadmap](../roadmap.md) states planned work and objective exit gates. The
[status ledger](../roadmap.status.md) is the authority for what has been accepted. **A decision
recorded here is not evidence that it was implemented**, and a record whose subject the checkout
does not contain says so in its own text.

| Record | Decides | Milestone |
|---|---|---|
| [JSD-0001](0001-placement-identity-and-assembly-topology.md) | Where this component lives, its profile ID and package identity, and the three assemblies it is built from | JS-0 |
| [JSD-0002](0002-feature-manifest-allocation.md) | The feature-manifest identities, what each admits, and the admission criterion for the next increment | JS-0 |
| [JSD-0003](0003-deployment-composition-labels.md) | The three composition labels, what each contains, and which are advertised | JS-0 |
| [JSD-0004](0004-limit-defaults-hard-maxima-and-the-budget-matrix.md) | The fifteen limit defaults, the fifteen hard maxima, the budget declaration matrix, and which numbers are measured rather than chosen | JS-0 |
| [JSD-0005](0005-the-seed-waited-on-set-and-snapshot-stop-condition.md) | Per-item dispositions on the seed's open work, the snapshot stop condition, the satellite dependency, and the nullable and unsafe positions | JS-0 |
| [JSD-0006](0006-assurance-evidence-and-rules-adoption.md) | That this profile adopts the component's assurance system, rule register, API baseline and evidence contract rather than standing up its own, and what that costs | JS-0 |
| [JSD-0007](0007-cross-profile-position-and-amendment-grading.md) | The `WebAssembly` host-object surface as a named exclusion, the refusal of a cross-profile value channel, and the re-grading of the amendment candidates | JS-0 |
| [JSD-0008](0008-format-version-1-the-entry-point-and-what-js-1-corrected.md) | What format version 1 carries, the entry-point answer, and four things JS-1 corrected in earlier records | JS-1 |
| [JSD-0009](0009-the-diagnostic-registry-and-the-position-encoding.md) | The published diagnostic-code registry, its two halves, the three codes no artifact reaches, and this profile's use of the core's position record | JS-3a |
| [JSD-0010](0010-which-review-rules-govern-this-profiles-documents.md) | That the component's review-document rules read this profile's ledger and bundles, that the mark vocabulary is per document family, and the two clauses that do not govern them | JS-3a |
| [JSD-0011](0011-the-value-frame-and-call-abi.md) | The eight-row value, frame and call ABI, and the re-scoping of JS-6 from a copy to a rewrite that follows from it | JS-4, entry gate |
| [JSD-0012](0012-the-profile-api-baseline-and-where-its-clause-lives.md) | The family's own frozen public surface, described without loading anything, and the re-homing of the gate clause that asked for it | JS-3a |
| [JSD-0013](0013-the-fuzz-sessions-coverage-signal.md) | What a fuzz session observes as its coverage signal, why instrumenting for edge coverage is refused, and what a session judges about itself instead of its growth | JS-9 |
| [JSD-0014](0014-the-source-front-end-and-the-verification-boundary.md) | Where the verification boundary falls, that static semantics is one stage, where strict mode is ruled on, that parse options are a value, and what a nesting case gets | JS-3b |
| [JSD-0015](0015-the-conformance-oracle-and-what-it-refuses-to-score.md) | Where the conformance harness lives and why its non-advertisement is a rule, how a suite is pinned, that every case runs in a runtime of its own, and the six named ways a run is misconfigured | JS-3a |
| [JSD-0016](0016-ingesting-a-third-party-suite-and-the-refusals-that-answer-nothing.md) | The dialect a real suite is read in, the two strictness readings of one file, and the rule that a refusal answers a question about the language only when it was a language answer | JS-3a |
| [JSD-0017](0017-the-end-user-host-and-what-an-exit-code-promises.md) | The end-user host: that it is a host and not the toolchain the core defers, the seven exit codes and which two of them accuse this component, how a file is read honestly, and why the first root a person would expect to be shipped is not | JS-3b |
| [JSD-0018](0018-which-tests-are-about-this-language-and-who-decides.md) | That the suite's own feature list decides which constructs are proposals, that reading it is required rather than offered, and that the exclusion is its own counted stage asked before the run's own filter | JS-3a |
| [JSD-0019](0019-the-pinned-language-edition-and-what-two-of-three-actions-buy.md) | The language-specification edition pinned by commit and digest, why a tag and the errata revision were both refused, what retrieval and hashing buy while the archive is outstanding, and the one place the pin and the conformance suite's own feature list disagree | JS-3a |
| [JSD-0020](0020-the-retained-conformance-suite-pin-and-the-one-it-replaces.md) | The conformance suite pinned by upstream commit, content digest and file count, held where the suite cannot reach it, why the pin it replaces was self-certifying rather than merely transient, why the reading was taken twice and the second independently of the first, and the archive and the floor this record refused and its own header records as overtaken hours later | JS-3a |
| [JSD-0021](0021-the-wide-bring-up-manifest-and-format-version-2.md) | The second manifest identity `broiler.javascript.wide` and the format version defined against it, why the slice was not widened and the core name not taken, why a version break rather than more opcodes, the one verifier both versions are read through, and the condition that retires or narrows this identity | JS-5 |
| [JSD-0022](0022-what-the-nesting-bound-bounds-and-the-stack-it-is-derived-against.md) | That the nesting bound stays a bound on the parser's recursion and a second bound is minted on the depth of the tree handed to a walk, the stack this component declares for compilation that its ceiling is derived against, why the ceiling is not a host's to raise, and what refusing a deeper tree costs | JS-3b |
| [JSD-0023](0023-which-pauses-route-through-a-core-suspension.md) | Which of section 12's three pause kinds route through a core suspension and which stay this profile's own job records, why external suspension is left undeclared at core contract version 1, and the live-suspension count the routing produces | JS-7 |
| [JSD-0024](0024-the-in-realm-host-surface.md) | That an embedder installs host objects through this profile's own realm seam rather than through the capability channel, that the capability table is the permission for that seam rather than its channel, and what charging, bracketing and an abort that cannot be swallowed have to hold for the permission to mean anything; *amended 2026-09-22 (section 19, JSeal B06)*: a BigInt host value kind with exact conversion, value equality, `ToBoolean` and the `SurfaceDeclined` refusal; *amended 2026-09-22 (section 20, JSeal I11/I12-upstream)*: `TryGetModuleState`, the referrer of eval and `Function` code (optional section `ScriptReferrers`), `AllowTopLevelAwait` honoured, and `Missing` refused or `undefined` at every crossing | JS-5 |
| [JSD-0025](0025-the-baseline-native-form-over-the-wide-manifest.md) | That the wide manifest has a native form of its own - every unit emitted, the control flow between instructions emitted, and every instruction one call into the interpreter's own dispatch for that instruction - where that form's references are rooted when its emitted frame holds none, why the computing and delegating forms were weighed and both kept, what the form costs and what would falsify it, and why no core amendment is needed | JSB-11 |
| [JSD-0026](0026-the-direct-eval-environment-boundary.md) | **Proposed, unsigned; steps 1-5 implemented by JSeal V14, steps 6-9 by JSeal V15, and a body's own variable environment, `super` and private names in evaluated source by JSeal V15-finish; module-scoped sites still refused.** What a direct `eval` carries into dynamic compilation - a verified per-site scope map in the caller's artifact, an eval request with the caller's strictness and syntactic permissions, run-time lookup of the caller's live records - the by-name record for `var`s an evaluation introduces, compile permission on every route, and the ordered steps that replace the function-scope refusal without an intermediate wrong answer | none; JSeal V13 (implementation V14-V15 and V15-finish all landed 2026-09-21) |
| [JSD-0027](0027-intl-scope-and-data-strategy.md) | **Proposed.** Intl stays deferred and absent - no empty or partial `Intl` object - with the locale-named methods' actual fallbacks recorded as the wide manifest's fixed locale row, three ECMA-262 fallback defects split out (N1-N3), and consumer-triggered Collator, NumberFormat and DateTimeFormat slices over pinned CLDR data; the data source, licence and size budget are owner decisions pending | none; JSeal D01 |
| [JSD-0028](0028-shared-memory-and-atomics.md) | **Proposed.** The `SharedArrayBuffer`/`Atomics` exclusion is kept, with what an agent model would have to decide - ownership, shared accounting, atomic element access, wait/wake lifetime, cancellation and embedding policy - before any of it could be reopened in staged gates | none; JSeal D02 |
| [JSD-0029](0029-finalization-registry-cleanup-model.md) | **Proposed.** The inert `FinalizationRegistry` stays the default; optional cleanup is a metered sweep at the host's drain point with deterministic eligibility seams, and guest code never runs from a CLR finalizer | none; JSeal D03 |
| [JSD-0030](0030-shadowrealm-support-boundary.md) | **Proposed.** `ShadowRealm` is deferred behind a pinned proposal revision; a child realm inherits its parent's compilation policy, callable wrapping and the primitive-only boundary are specified, and bounded follow-ups are named | none; JSeal D04 |
| [JSD-0031](0031-unicode-data-source-and-build-boundary.md) | **Proposed, not taken.** Unicode 17.0.0 as the pinned data version for normalization (F08) and property escapes (F09), archived UCD inputs, a deterministic generator/check pair and no network at build time. *Amended 2026-09-22:* U1 (archive) and U2 (generator, rules N22 and N23, tables) performed; the owner's licence and size decisions given in conversation and recorded unsigned (section 9); U3 (F08, `normalize` in all four forms) built (section 10); U4 (F09, property escapes and the `u`-mode Canonicalize on `CaseFolding.txt`) performed (section 11); the "later, separately" items - `ID_Start`/`ID_Continue` and `WhiteSpace` in the tokenizer, group names, the non-`u` Canonicalize, a `u` `lastIndex` inside a pair, early flag errors - performed (section 12) | none; JSeal F07, F08, F09, JSD-0031-later |
| [JSD-0032](0032-the-internal-structured-clone-carrier.md) | **Proposed; internal implementation in the tree.** The structured-clone carrier is detached data (records, slots, copied bytes; no realm handles), adoptable more than once after its source is disposed (single-use once it holds transferred bytes), internal and reached by the slice-compiler root's checks; the getter and Error rules, the fuel and size bounds, the supported-type matrix with BigInt, resizable and shared buffers' entry conditions, and ArrayBuffer transfer validated completely before any detachment; since I17 one public door (`DetachClone`/`AdoptClone`, opaque `JsHostCloneCarrier`, JSD-0024 section 17) with the thread, lifetime, single-use and foreign-carrier rules, and the sender paying `LiveBytes`; no `WorkerRealms` claim; *amended 2026-09-22 (B06)*: a BigInt slot and a BigInt-object record, carrier layout 2 | none; JSeal I14-I17, B06 |
| [JSD-0033](0033-the-internal-bigint-value-and-the-gated-literal.md) | **Proposed; internal implementation in the tree.** BigInt as an internal `JsBigInt` over the BCL's `BigInteger` in the existing 24-byte value; a two-half gate (an internal compile flag, and the known-but-unadvertised `broiler.javascript.bigint` surface that every public descriptor door refuses and only an internal door admits) that keeps the wide manifest's named refusal for every public compilation; a canonical, 65,536-bit-bounded constant tag 7 that older readers, and well-formed ungated artifacts, refuse as an unknown tag; budgets, the value ceiling, and what each operation answers or refuses until B03-B06; *amended 2026-09-21 (section 7, B05)*: the surface is admitted - `broiler.javascript.bigint` joins the surfaces the full descriptor admits and stays declinable, the `BigInt` global and prototype, exact conversions and mixed comparisons, JSON's TypeError, three update opcodes (`0xB0`-`0xB2`) in format version 2, and a hand re-base of the wide floor's unsupported row | none; JSeal B01-B05 |
| [JSD-0034](0034-admitting-explicit-resource-management-ahead-of-the-edition.md) | **Proposed; implementation in the tree.** Explicit resource management (the disposal Symbols, `SuppressedError`, both stacks, `using` and `await using`) is admitted as a whole ahead of the pinned ES2026 edition, against `tc39/proposal-explicit-resource-management` at `38c13295` (text not archived); the `--test262` command scores the `explicit-resource-management` flag and still skips every other proposal; the five disposal instructions `0xA0`-`0xA4`, and the budget policy that no disposer runs once an allowance is spent | none; JSeal F21-F22 |
| [JSD-0035](0035-the-value-form-emitted-semantics-over-nan-boxed-values.md) | **Proposed; stages JSV-0 (the word, codec, handle table, scan and handle-stress) and JSV-1 (the form on an execution path, every instruction a helper, control emitted) in the tree, JSV-1's gate open on `zlib`, which runs past the profile's wall-clock hard maximum in this form; nothing after them implemented.** A third output form over the wide manifest whose emitted code executes the pure instructions itself over 64-bit NaN-boxed words in a pinned per-instance slab, with references held as generation-checked indices into a per-instance handle table rooted by a precise tag scan and exercised by a handle-stress mode; every other instruction runs the interpreter's own per-opcode arm over a decoded mirror window; direct calls between value-form units; fuel settled as a debt after pure instructions, so verdicts agree at every ceiling without a meter amendment. It would amend JSD-0011 Rows 1 and 2 for that one region and take MVP-8's alternative branch, on a predeclared measurement | none yet (JSV, if adopted) |

## What a record must carry

Each record states its **status**, its **date**, its **owner** and any **co-signer**, the
**decision**, what it **rejects and why**, and — where the decision is provisional — the named
condition that would settle it and the milestone that owns settling it. A decision with no
recorded rejection is a decision nobody chose between.

**Where owner and co-signer are the same person, the record says so in those words.** The
roadmap requires the non-independence to be recorded as a limit on what the decision proves,
not resolved by assertion, and every record in this series is currently in that position.
